[project]
name = "official-act-rgbd"
version = "0.1.0"
description = "Depth frontend benchmark on the unmodified RoboTwin ACT action core"
requires-python = ">=3.10"
dependencies = [
  "h5py",
  "numpy",
  "opencv-python",
  "torch",
  "torchvision",
]

[tool.ruff]
line-length = 120

